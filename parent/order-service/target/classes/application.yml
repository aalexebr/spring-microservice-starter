spring:
  application:
    name: order-service
  datasource:
    url: jdbc:mysql://localhost:3306/test-order-service?createDatabaseIfNotExist=true&autoReconnect=true&useSSL=false
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop
  cloud:
    config:
      enabled: false
server:
  port: 8997


  
